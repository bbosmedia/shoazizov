declare module 'react-animated-cursor' {
	export default function AnimatedCursor({ innerSize, outerSize, color, outerAlpha, innerScale, outerScale, trailingSpeed, outerStyle}: { innerSize: number; outerSize: number; color: string; outerAlpha: number; innerScale: number; outerScale: number; trailingSpeed?: number, outerStyle: any })
}
